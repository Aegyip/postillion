{{--
  Giriş, kayıt ve doğrulama ekranlarının ortak çerçevesi.

  Marka işareti burada, sayfalarda değil: üçü de aynı yerde durmalı, yoksa
  ekranlar arasında geçerken logo bir piksel oynar.
--}}
@component('components/layout', { title, captchaScript })
  <main class="auth">
    <a class="brand" href="/">
      <img src="/assets/postillion-app-icon.svg" alt="" width="26" height="26" />
      Postillion
    </a>
    {{{ await $slots.main() }}}
  </main>
@end
